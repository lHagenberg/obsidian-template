        
# to read:
```tasks
filename includes @
filter by function \
    const symbol = task.status.symbol; \
    return symbol === 'r';
```

# recently read:
```tasks
filename includes @
filter by function \
    const symbol = task.status.symbol; \
    return symbol === 'R';
done in or after last month
sort by done date
```
