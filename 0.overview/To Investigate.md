
# to investigate:
```tasks
filter by function \
    const symbol = task.status.symbol; \
    return symbol === '?';
```

# investigating:
```tasks
filter by function \
    const symbol = task.status.symbol; \
    return symbol === 'i';
```

# investigated:
```tasks
filter by function \
    const symbol = task.status.symbol; \
    return symbol === 'I';
```